1. Run datasets.py to convert csv to npy with format  
            {
                'tv_hist': tv_positions,             # (TH, 2)
                'tv_vel': tv_velocities,             # (TH, 2)
                'nv_sp': np.array(nv_sp),            # (N, TH, 2)
                'nv_dp': np.array(nv_dp),            # (N, TH, 2)
                'center_frame': center_frame,
                'tv_id': tid
            }

python traffic-analysis-detection-tracking/SDTATT/datasets.py

2. Dataloader.py class SDTATTDataset loads the .npy file and creates tensor of each key 

3. model.py has the model SDTATT

4. eval.py runs the pipeline, makes prediction and overlays the prediction of target vehicle on the video

USER@DESKTOP-9HMMGG6 MINGW64 /c/Agam/Work/vehicle_trajectory_prediction (main)
python traffic-analysis-detection-tracking/SDT-ATT/eval.py


